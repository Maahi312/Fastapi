services:

  postgres:
    image: postgres
    container_name: postgres_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Mahesh%40204
      POSTGRES_DB: shared_models
    volumes:
      - postgres-data:/var/lib/postgresql/data

  pgadmin:
    image: pgadmin
    container_name: pgadmin_db
    restart: unless-stopped
    ports:
      - "5050:80"
    environment:
      PGADMIN_EMAIL: mahesh.darimireddy1997@gmail.com
      PGADMIN_PASSWORD: Mahesh123
    depends_on:
      - postgres

volumes:
  postgres-data:
